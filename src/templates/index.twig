{% extends '_layouts/base.twig' %}

{% block content %}

<div class="bg-gray-800">
    <div class="container mx-auto py-4">
        <div class="w-full">
            <h1 class="font-bold text-white text-2xl">{{ page.title }}</h1>
        </div>
    </div>
</div>

<div class="container pt-24">
    <div class="row">
        {% include "_components/grid.twig" %}
    </div>
</div>

{% endblock %}