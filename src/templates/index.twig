{% extends '_layouts/base.twig' %}

{% block content %}

<div class="bg-green-500">
    <div class="container mx-auto py-4">
        <div class="w-full">
            <h1>Dis is {{ cta.title }}</h1>
        </div>
    </div>
</div>

{% endblock %}